import { MarkerType } from '@xyflow/react';

export const markerStyles = { width: 20, height: 30, color: '#ff4d41', type: MarkerType.ArrowClosed };

export const defaultMarkerStyles = {
  markerStart: markerStyles,
  markerEnd: markerStyles,
};
